import { Inter, Roboto, Jost } from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] });
export const roboto = Roboto({weight: "300", subsets: ['latin']});
export const jost = Jost({ subsets: ['latin']});